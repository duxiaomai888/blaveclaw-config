# Shared Library (lib/)

The workspace has a shared library at `lib/`. Use it to avoid duplicating code across strategies.

**Always import these — never write them inline:**

Every `fetch_*` function takes `headers` as its auth argument. Build it once from the workspace `.env` — the keys are lowercase, and the header names are `api-key` / `secret-key` (not `X-API-KEY`, and there is no request signing):

```python
from dotenv import dotenv_values
env  = dotenv_values()   # run from the workspace root, or pass the .env path
hdrs = {'api-key': env.get('blave_api_key', ''), 'secret-key': env.get('blave_secret_key', '')}
```

`lib/data.py` — all data fetching (chunking + cache built-in):
- `fetch_db_kline(dataset, symbol, schema, start, end, headers)` → CME/NYMEX/ICE OHLCV + `instrument_id` column; datasets: `GLBX.MDP3` (CL, GC), `IFEU.IMPACT` (BRN); schemas: `ohlcv-1m` / `ohlcv-1h` / `ohlcv-1d`
- `settlement_signals_from_db(df, signal)` → returns `(signal, exec_at_close)`. For futures strategies using `fetch_db_kline`: call at the end of `compute_signals` and `return` the result directly. Forces `signal=0.0` on the last bar before each contract rollover, and marks those bars `exec_at_close=True` (executed at this-bar close, not next-bar open). Do NOT use `-1.0` for settlement — that opens a short position.
- `fetch_kline(symbol, interval, start, end, headers)` → OHLCV DataFrame (Open/High/Low/Close/Volume — Volume is real base-asset volume; individual bars may have `NaN` Volume when the source is missing it, so volume-based logic must handle NaN itself — `dropna`/`fillna`, never assume all-finite). **Binance USDT-M perps only** — a contract listed elsewhere is simply absent, so a symbol that "works" is not proof it is the user's instrument. All intervals (`1min`–`4min` included) reach back to the symbol's Binance um-futures listing date — the API backfills old months from Binance's official archive; a window before listing returns empty rows, not an error. First deep sub-5min fetch pulls history month-by-month (30-day chunks server-side), so expect the cold run to take a while; it lands in the local monthly cache and is fast afterwards
- `fetch_bingx_kline(symbol, interval, start, end)` → OHLCV DataFrame for a BingX perpetual, straight from BingX's public API (no `headers` — no key needed). Use for contracts `fetch_kline` does not carry. `symbol` is the BingX **API** symbol, not the chart's display name: GOLD(XAU)-USDT is `NCCOGOLD2USD-USDT` — resolve it via `GET https://open-api.bingx.com/openApi/swap/v3/quote/contracts` (match `displayName`, use `symbol`). Intervals: `1min`/`3min`/`5min`/`15min`/`30min`/`1h`/`2h`/`4h`/`6h`/`8h`/`12h`/`1d`/`3d`/`1w`
- OHLC fetchers (`fetch_kline`, `fetch_db_kline`, `fetch_twfutures_ohlcv`, `fetch_twstock_price`) drop bars with impossible values (high<low, non-positive or NaN price) at read time and print the dropped timestamps — a printed `⚠️ dropped N bar(s)` warning means upstream data was corrupt, not a fetch failure; the cache keeps the raw bars
- `fetch_holder_concentration(symbol, interval, start, end, headers)` → DataFrame with `alpha` column
- `fetch_funding_rate(symbol, interval, start, end, headers)` → DataFrame with `alpha` (Binance only; alpha = funding rate × 100)
- `fetch_taker_intensity(symbol, interval, start, end, headers, timeframe='24h')` → DataFrame with `alpha`
- `fetch_whale_hunter(symbol, interval, start, end, headers, timeframe='24h', score_type='score_oi')` → DataFrame with `alpha`
- `fetch_unusual_movement(symbol, interval, start, end, headers, timeframe='24h')` → DataFrame with `alpha`
- `fetch_squeeze_momentum(symbol, start, end, headers)` → DataFrame with `alpha` (period fixed to 1d)
- `fetch_liquidation(symbol, interval, start, end, headers, timeframe='24h')` → DataFrame with `alpha`
- `fetch_market_direction(interval, start, end, headers)` → DataFrame with `alpha` (no symbol)
- `fetch_capital_shortage(interval, start, end, headers)` → DataFrame with `alpha` (no symbol)
- `fetch_market_sentiment(symbol, interval, start, end, headers)` → DataFrame with `alpha`
- `fetch_top_trader_exposure(interval, start, end, headers)` → DataFrame with `alpha` (BTC only, no symbol)

**Data coverage & scale — measured facts, do NOT probe the API first.** When the user has not fixed a START (or you need the full history), request from the dataset's start date below — never widen a START the user gave; the actual first bar for a given symbol is simply `df.index[0]` (= max(dataset start, listing date) — e.g. DOGEUSDT 2020-07-10, SOLUSDT 2020-09-14, 1000PEPEUSDT 2023-05-05; HC for late listings can start months after listing). Every alpha below is on a z-score-like scale (mean≈0, std≈1) unless stated, so the scale is the same for every symbol. Measured 2026-08-22 on BTC/ETH/DOGE/SOL/1000PEPE, `1d` and `1h` both available:
- `fetch_kline` — from **2020-01-01** (or listing); price, not a z-score. The `1d` series includes today's still-open bar (not closed until 00:00 UTC).
- `fetch_holder_concentration` — from **2021-05-11** (BTC/ETH; DOGE/SOL 2021-05-28; 1000PEPE 2024-02-27); mean≈0, std≈1.2, p1≈−2.4, p99≈+3, extremes ≈−5…+11 (`1h` tails fatter).
- `fetch_taker_intensity` — from **2020-01-01** (or listing); std≈1.1, p1≈−3.4, p99≈+2.5…3, extremes ≈−10…+11.
- `fetch_whale_hunter` — from **2021-12-01** (or listing month); std≈1.05–1.1, p1≈−2.7, p99≈+3…4, `1h` extremes ≈±25.
- `fetch_liquidation` — from **2020-01-01** (or listing); mean slightly negative (−0.0…−0.2), std≈0.8–1.1, p1≈−3.3, p99≈+2.5, `1h` fat tails to ±65.
- `fetch_funding_rate` — **only from 2025-09-30** (≈11 months of history — say so before proposing a long backtest); NOT a z-score: funding rate × 100, mean≈0.002–0.003, std≈0.005–0.02, extremes ≈−0.3…+0.07.
- Daily alphas end at the **last closed day (yesterday)** — that is not a lag, it is the last complete bar; joining onto today's open `1d` kline bar leaves that row's alpha NaN (a blind `ffill` would carry yesterday's value onto it). Hourly alphas and kline run to the current hour.
- Not yet measured (`unusual_movement`, `squeeze_momentum`, `market_direction`, `capital_shortage`, `market_sentiment`, `top_trader_exposure`): fetch from 2020-01-01 and read `df.index[0]`; one call answers the question.

**Data-depth discipline (why AGENTS.md makes this absolute):** how far back a dataset reaches is a platform fact that changes over time (sub-5min crypto klines were capped at 45 days, then extended to each symbol's listing date), and a remembered limit outlives the change: an agent that hit `only goes back 45 days` in the morning kept telling the user "1m cannot do two years" all afternoon — after the platform had already lifted the cap and the local lib no longer contained the check — because the rolling conversation memory, a note in the strategy file, and a skill doc that had not yet re-synced all still said so. Skill docs re-sync once a day, so they can lag a platform change by up to a day; conversation memory never expires on its own. So before asserting a depth limit, probe it live, once, narrowly:

```python
# a few days at the deep end of the range the user wants — one call, not a backtest
df = fetch_kline('BTCUSDT', '1min', '2024-01-01', '2024-01-03', headers)
print(len(df), df.index.min() if len(df) else 'empty')
```

Scope: history depth only. Per-request window caps (twstock kbar 31 days, TXF per-schema ranges, sub-5min 30 days) are real and handled by the lib's chunking — never probe those. An empty probe is the verified answer for immutable sources; for sources that backfill progressively (twstock minute lines seed ~30 recent days on first query and deepen afterwards — see `references/twstock.md`) report the documented coverage, not "no data". Then fix the stale note in the strategy file so the wrong limit does not get re-read next turn.

**Taiwan stock price — raw vs adjusted (critical distinction):**
- `fetch_twstock_price(sid, start, end, headers)` → Open/High/Low/Close/Volume, **actual market prices**. Use when the user asks to plot or view a stock's trend — matches what they see on broker apps.
- `fetch_twstock_institutional(sid, start, end, headers)` → per-stock daily frame with `foreign_net` = `foreign_buy − foreign_sell` in **shares (股)** — divide by 1,000 for 張 (the api reads TWSE 外陸資買進股數); other raw columns pass through
- `fetch_twstock_price_adj(sid, start, end, headers)` → Open/Close, **dividend-adjusted backward prices**. Use for backtesting only — ensures returns are comparable across ex-dividend dates.
- Never use `fetch_twstock_price_adj` just to draw a chart; the adjusted prices look different from real market prices and will confuse users.
- `fetch_twstock_quote(sid, headers)` → dict, real-time last-quote snapshot (~10s refresh); no history, "now" only
- `fetch_twstock_quote_batch(stock_ids, headers)` → dict `{stock_id: quote_dict}`; max 50 ids
- `fetch_twstock_ohlcv(stock_id, schema, headers, start=None, end=None, adjust=False)` → Taiwan stock minute-line OHLCV DataFrame (Open/High/Low/Close/Volume); schema: `'1m'`/`'5m'`/`'15m'`/`'30m'`/`'60m'`/`'1d'`; **Volume in lots (張)**; bars are minute-start labels (13:30 Taipei bar = closing auction); `adjust=True` = forward-adjusted (後復權) OHLC for backtests across ex-dividend dates (volume unchanged; 503 fail-loud if factors unavailable); history from 2019-01-01 (earlier `start` silently clamped); whole market backfilled server-side — a freshly listed stock's first query may return only recent data, deep history follows by the next day, and locally cached empty months auto-retry after 24 h (see `references/twstock.md`)
- `fetch_twstock_ohlcv_symbols(headers)` → list of stock_ids that already have minute-line data server-side; any listed stock_id can still be queried (first query auto-seeds)
- `fetch_twstock_market_value_all(headers, top=None)` → DataFrame `rank`/`stock_id`/`name`/`market_value` (NTD, int), whole-market market-cap ranking (上市+上櫃+ETF, ~2,400 rows) in one call; `top` = first N ranks (1–3000, validated locally), None = all; as-of date in `df.attrs['date']`; EOD daily, full ranking cached 1 h locally and sliced; drop ETFs via `stock_id.str.startswith('00')`. Use for 前十大權值股 / market-cap top-N pools — never rebuild from per-stock shares × price (see `references/twstock.md`)

- `twstock_industry_name(code)` → the 產業別 name for a `fetch_twstock_list` `industry_code` (`'24'` → `'半導體業'`; `24` and `'5'` work too). None/NaN (ETFs) → None. An unknown code comes back unchanged; never guess a name. The full 35-code map is `TWSE_INDUSTRY_NAMES`, and listed and OTC companies share the codes. `'91'` = depositary receipts (存託憑證) is a Blave label, because TWSE publishes none for it.

Taiwan stock data (universe, batch functions, fundamental factors, lookahead-bias table) — **`references/twstock.md`**

**Taiwan market calendar — the only source for "is the market open on X":**
- `fetch_twstock_holidays(headers, year=None)` → DataFrame `date`/`name`/`type`/`note`, one row per day listed in TWSE's annual holiday schedule (`year` defaults to this Taipei year). `type` is `'holiday'` or `'settlement_only'` (no trading, settlement only, e.g. the two days after the lunar-new-year close). Trading-day marker rows are already removed, and weekend dates may appear. `df.attrs`: `source` / `source_zh` (attribution), `note`, `stale` (TWSE was unreachable, so this is the last stored copy), `year`. **Returns None** when the endpoint is unreachable or TWSE has not published that year yet (the 2026 table was published in late October 2025, one observation). None means unknown: say 「TWSE 尚未公布該年度休市表」 or 「休市表目前取不到」. Never say "no holidays", and never fill in a date from memory.
- `is_tw_trading_day(date, headers)` → True / False / None. Saturday and Sunday → False without a fetch. A `'holiday'` or `'settlement_only'` row → False. None = unknown, as above.
- **The table excludes ad-hoc closures.** Typhoon days are never in it (2026-07-10 is not). True only means "not a scheduled closure", so never promise 「明天一定開市」 from it. Only that day's own data (an index close for the date) proves a session took place.
- **The TXF night session does not follow the day calendar.** It still runs on the evening before a single-day holiday and on the year's last trading day (封關日), and its trades settle to the next trading day's session. A holiday closes the day session, not the evening before it. The settlement rule is TAIFEX's; the evening-before behaviour is observed in our TXF 1-minute data and in TAIFEX's own 2026-02-11 after-hours row.
- **Attribution is the licence condition.** Any report or reply that cites the table carries `df.attrs['source_zh']` (zh) or `df.attrs['source']` (en) verbatim in its footnote. Do not shorten it or rename the agency, and keep it in the footnote even when the report is shared. Use this function only; never fetch twse.com.tw from this machine.

- `fetch_twfutures_ohlcv(symbol, schema, start, end, headers)` → Taiwan futures OHLCV DataFrame (Open/High/Low/Close/Volume); symbol: `'TXF'` (`'MXF'`/`'TMF'` auto-alias to the `'TXF'` series — see the instrument note below) or an individual stock futures id (股票期貨, e.g. `'CDF'`) currently backfilled with minute-line data — most of the 231 are NOT, unsupported symbol → 400; check `fetch_stock_futures_ohlcv_symbols()` first; schema: `'1d'`/`'1m'`/`'5m'`/`'15m'`/`'30m'`/`'60m'`; Volume in contracts. Intraday spans >62 days automatically use the server's 1m-parquet bulk export (one request per calendar year, resampled locally — fast, no per-request server computation) and silently fall back to chunked JSON fetches if that endpoint is unavailable
- `fetch_twfutures_ohlcv_batch(symbols, schema, start, end, headers, max_workers=8)` → dict `{symbol: DataFrame}`; concurrent multi-symbol form of `fetch_twfutures_ohlcv` (same cache/export semantics per symbol); use for any universe-wide intraday fetch — serial per-symbol loops waste ~2s of auth overhead per request
- `fetch_stock_futures_ohlcv_symbols(headers)` → list of symbols currently allowed by `fetch_twfutures_ohlcv` (always includes `'TXF'` plus whatever stock futures ids currently have backfilled minute-line data); call this before relying on intraday data for a stock future
- `fetch_twfutures_bid_ask_vol(start, end, headers)` → TXF 1-min bid/ask volume DataFrame (bid_vol, ask_vol, total_vol); bid_vol = 內盤 (seller-initiated), ask_vol = 外盤 (buyer-initiated); includes day + night sessions; max 31 days per chunk (auto-chunked)
- `fetch_twfutures_pcr(start, end, headers)` → DataFrame with a single `pcr` column (daily, index `date`); official TAIFEX put/call ratio (OI-based, 買賣權未平倉量比率%); the official ratio — NOT the value derived from option institutional / large-trader data in `references/twfutures.md`.
- `fetch_twfutures_institutional(futures_id, start, end, headers)` → 期貨三大法人 daily DataFrame, 12 float columns in 口數: `{foreign|investment_trust|dealer}_{net_oi|long_oi|short_oi|net_deal}` (`foreign_net_oi` = 外資期貨淨多單); `futures_id` `'TX'`/`'MTX'`/`'TMF'` (微台 has its own series — do not substitute `MTX`; `'TE'`/`'TF'` unverified), execution names auto-map (`'TXF'`→`'TX'`, `'MXF'`→`'MTX'`); stock futures 404; cached one-file-per-id like the `twmarket_*` series; rows for day D land after ~15:00 TWN
- `fetch_stock_futures_batch_daily(futures_ids, start, end, headers)` → dict `{futures_id: DataFrame}`; same fields as `fetch_twfutures_daily`; max 250 ids per call; `futures_ids` must be valid stock futures ids (股票期貨, e.g. `'CDF'`) — see `references/twfutures.md` for the minute-line-coverage caveat
- `txf_settlement_mask(index)` → boolean Series, True on the last bar strictly before each TAIFEX monthly settlement (3rd Wednesday, 13:30 TWN); interval-agnostic (1m → 13:29 bar, 60m → 13:00 bar). **MANDATORY for every strategy on `fetch_twfutures_*` intraday data — TXF and stock futures alike.** The source is Shioaji's R1 continuous near-month series, which switches contracts at settlement without price adjustment; an unmasked position books the contract-basis gap as fake PnL (measured 2018-2026 across 10 stock futures: mean +0.36% per roll, std 3.9%, August dividend-season mean -1.9% — a fully-invested unmasked backtest fabricates roughly +4%/yr × gross exposure). Masking also charges the real monthly roll fees the naive backtest omits. Type A: `settle = txf_settlement_mask(df.index); signal[settle] = 0.0; return signal, settle`. Type C: `weights.loc[settle] = 0.0; return weights.values, price_df, settle.values`. Machine-enforced: `lib/quality_check.py` flags a TAIFEX strategy without the mask as CRITICAL, and `run()` refuses to backtest one (live/cron runs are not blocked).
- **Taiwan index futures — `SYMBOL` declares the execution instrument (TXF / MXF / TMF):** a strategy sets `SYMBOL` to the contract it actually TRADES — `'TXF'` 大台 (200), `'MXF'` 小台 (50), `'TMF'` 微台 (10 TWD per index point); pick by the user's capital and sizing granularity. The data layer aliases automatically: `fetch_twfutures_ohlcv` maps `'MXF'`/`'TMF'` to the `'TXF'` series (shared cache), because signals and backtests always use TXF data — arbitrage pins 大台/小台/微台 to the same price, TXF has the deepest and cleanest minute history, and the server has no TMF minute data at all (daily + institutional only). So `SYMBOL = 'TMF'` simultaneously means: backtest on TXF bars, trade the micro contract (TM0000 at Capital — the broker order code and `contract_value` derive mechanically from `SYMBOL`, see `references/capital-broker.md` Step 8). What DOES change per instrument is `FEE`: the futures transaction tax is proportional (0.002% per side, identical for all three) but commission is a fixed TWD amount per contract, so its share of notional grows as the contract shrinks. **`FEE` is PER-SIDE** — the engine charges `|Δw|·FEE` on every position change, so a round trip is charged twice; never put a round-trip total into `FEE`. Verified 2026-08 (index 44,930; retail net-order commission ≈ 55 / 28 / 18 TWD per side): explicit per-side cost ≈ TXF 0.003% / MXF 0.003% / TMF 0.006%; adding 1 tick slippage per side ≈ 0.005% / 0.006% / 0.008%. Commissions are fixed, so these fractions shrink as the index rises — recompute at the current level: `FEE ≈ 0.00002 + commission/(multiplier·index) + tick/index`. The 0.03% default in AGENTS.md is a conservative ceiling for all three; for 1m / high-turnover strategies use the per-instrument figure, or the backtest overstates cost drag ~4–6×. Caveat: TMF night-session book depth is unverified — the 1-tick slippage figure is a day-session assumption.
**Macro economic calendar — the only source for macro events and their numbers:**
- `fetch_economic_calendar(headers, start=None, end=None, countries=None, max_priority=None, limit=None, lang='zh')` → DataFrame, one row per event, sorted by event time. Columns: `datetime` (Taipei time; events with no published time get 00:00), `date`, `time` (`'HH:MM'` Taipei, may be `None`), `country` (ISO-2) / `country_name` (Chinese), `subject` (indicator name) / `subject_title` (period, e.g. `'<7月>'`, `'<2季>'`), `predict` (market consensus), `last` (prior), `real` (actual; `None` until released), `unit`, `priority`.

  **Macro facts discipline (why AGENTS.md makes this absolute):** a generic web search lands on calendar-aggregator content farms whose tables are themselves wrong, and whatever the page does not cover gets filled in from training data — measured on a real digest, that produced a wrong prior for China's PMI (49.5 vs the actual 50.3, a published fact with one right answer), a prior belonging to a different indicator entirely, and a Q2 GDP consensus off by 0.7pp. Time-sensitive facts the calendar does not cover are worse: memory silently overrides what was just read (a weekly digest called the FOMC presser Powell's months after Warsh replaced him, and a later run wrote 「鮑威爾（Kevin Warsh）」 while the fetched page said Warsh). Web search is blocked outright on some machines — every such failure is exactly the moment the tempting move is to fill the gap from training data. Never present an unverified name or number as fact.
- **`priority` runs 1 → 3 with 1 the MOST important** (1 = non-farm payrolls, rate decisions; 3 = rig counts, used-car indices). `max_priority` keeps only `priority <=` it, so "just the big events" is `max_priority=1`, not 3. Getting this backwards silently returns ~979 rows of noise instead of the 85 that matter.
- Other filters: `start`/`end` are `'YYYY-MM-DD'` Taipei dates (inclusive); `countries` is a list of ISO-2 codes, e.g. `['US', 'CN', 'TW']`; `limit` truncates after sorting; `lang` (`'zh'` default / `'en'`) picks the display language for indicator and country names — the server only swaps names it has in its lookup table, so `'en'` returns a zh/en mix. Unfiltered the feed is ~1,400 rows — always filter.
- **Coverage is a rolling ~5-week window (roughly the past month plus the next few weeks), not a history archive.** A range outside it returns an empty DataFrame rather than an error — do not read that as "no events scheduled". For anything older, there is no endpoint; say so instead of substituting a remembered figure.
- **Never answer a macro-event question from a web search or from memory.** Search results land on calendar-aggregator content farms whose own tables are wrong (measured: one listed a published actual as the consensus), and any field the page misses gets filled in from training data — that is how a digest ended up with China's manufacturing PMI prior at 49.5 when the published figure was 50.3, and a Q2 GDP consensus 0.7pp off. If this call cannot answer it, say so rather than substituting a number.
- **Data history ranges** (earliest available date per endpoint) are NOT listed here — they live in the `blave-quant` skill / Notion API doc, which auto-update on each box. This config is baked in at provision time (no live-update path), so a start date copied here would silently go stale. Check the skill before assuming an endpoint's earliest date.

`lib/execute.py` — all order execution logic (state management + algo orders):
- `from lib.execute import update_state, load_state, save_state` — state management
- `state.json` schema: `{"position": float, "symbol": str}` — `position` is the current signal value (positive=long, negative=short, 0=flat); all deployment config (exchange, asset_spec) lives in `portfolio_config.json`, not in state
- `from lib.execute import run_twap` — TWAP execution engine (exchange-agnostic). The reconciler's execution dispatch (below) drives it automatically; call it directly only for standalone Type B tooling.
  - `run_twap(symbol, side, total_qty, duration_min, n_slices, place_slice_fn, twap_key, signal_price=None, send_telegram_fn=None, stop_event=None, notify_slices=True)`
  - `place_slice_fn(symbol, side, qty) → {'fill_price': float, 'fill_qty': float}` — raise on failure; 3 consecutive slice failures abort the run
  - `stop_event` (threading.Event): once set, remaining slices are skipped and the summary carries `aborted=True`; `notify_slices=False` silences per-slice Telegrams (start/errors/summary still send)
  - `twap_key`: **symbol+direction** key like `'btcusdt_long'` / `'btcusdt_short'` — NOT a strategy name. Reconciler nets orders per symbol, so there is no single strategy name at execution time.
  - Logs every slice + summary to `manager/twap/{twap_key}.jsonl` (under `manager/`, never `strategies/`)
  - `signal_price` (optional): price at signal time — used to compute `slippage_bps` per slice and in the summary
- `from lib.execute import load_twap_log` — `load_twap_log(twap_key)` returns `(slices, summaries)` from `manager/twap/{twap_key}.jsonl`; use to analyze how TWAP parameters (duration, n_slices) affect execution quality vs strategy signal price

**Execution styles (下單方式) — BUILT IN, do not hand-wire TWAP into the reconciler anymore.** `portfolio_config.json["execution"]` maps strategy name → spec; the reconciler template routes every order through `lib.execute.dispatch_order`, which resolves the spec and executes. No entry = market order (unchanged default).
  - Specs: `{"type": "market"}` | `{"type": "twap", "duration_min": 1–1440}` | `{"type": "chase"}` | `{"type": "custom", "module": "<name>"}` (the web offers market / TWAP 15 min / chase; other TWAP durations are chat-set only)
  - The web 下單設定 writes market/TWAP/chase specs itself (via the `execution` command) — when the user asks in chat, prefer pointing them at the web control; edit the config key directly only when they can't use the web.
  - **Resolution is per netted symbol order**: `reconcile()` nets strategies per symbol, so the largest-|contribution| strategy's spec decides for that symbol. One strategy per symbol (the normal case) behaves exactly per-strategy.
  - TWAP sizing is automatic: ~1 slice/minute, every slice ≥ $20; orders too small to slice fall back to a single market order. Slices are market orders through `lib/venue_wiring` — HALT, audit and broker attribution all apply.
  - **Chase (限價追價)**: post-only limit at our own side's best price, cancel-replace as the BBO moves, 45 s window, remainder then goes market — better average price than a straight market order, still guaranteed to converge. Zero user parameters. Requires the venue lib's limit layer (`references/exchange-connect.md` rule 6); a venue without it degrades to market, loudly.
  - TWAP/chase/custom run in a background thread: the reconcile loop keeps serving other symbols; repeat legs for an in-flight symbol are deferred (returned `False`); a mid-flight signal flip stops the execution before its next slice (chase also cancels its resting order) and the residual re-reconciles; completion touches `state/execution/kick` to trigger an immediate round and appends the fill summary to `manager/orders.jsonl`.
  - An unusable spec (missing module, bad duration) does NOT strand the position: it places a market order and tells the user via Telegram, every occurrence.
  - Auto-wired official venues only — Taiwan brokers (sinopac/president/capital) keep their hand-wired signed-diff reconcilers and never enter this path.

**Custom executors — "write my own execution style".** When the user wants an execution shape the standard options don't cover (front-loaded slicing, randomized intervals, price-reactive pacing), write `manager/executors/<name>.py` (name: `[a-z0-9_]{1,64}`) exporting:
  ```python
  def execute(symbol, side, total_usd, ctx):
      # side: 'buy' | 'sell'; total_usd: positive USD to execute
      remaining = total_usd
      while remaining > 20 and not ctx.stop_requested():
          fill = ctx.place_slice(min(remaining, total_usd / 10))
          remaining -= fill["fill_qty"]          # fill_qty is USD
          if not ctx.sleep(60):                  # False = stop requested
              return
  ```
  then set `{"type": "custom", "module": "<name>"}` for the strategy in `portfolio_config.json["execution"]`.
  - `ctx.place_slice(usd)` — one market slice of the order's own direction, returns `{'fill_price', 'fill_qty'}` (fill_qty in USD); raises on failure. `ctx.sleep(s)` — interruptible, returns False when the platform wants out (signal flip / HALT / overdue): **return promptly then** — a custom style shapes HOW the target is reached, never whether. `ctx.stop_requested()`, `ctx.mark_price()`, `ctx.notify(msg)`.
  - Everything still goes through the venue wiring: HALT, audit log, attribution, minimums. There is no way (and no need) to place raw orders from an executor.
  - Partial fills are fine — the reconciler re-diffs whatever is left after the executor returns. Do not loop forever "to be safe"; executions are expected to end (24 h hard deadline, then the platform requests stop and warns the user).
  - Keep executors small and boring; test the shape with a dry run of the math in `tmp/` first. Never write TWAP/execution logs under `strategies/`.

`lib/analysis.py`:
- `from lib.analysis import regime_analysis, plot_regime` — regime breakdown and regime chart

`lib/param_scan.py`:
- `from lib.param_scan import nice_grid, nice_step` — **build every scan axis with this.** `nice_grid(lo, hi, n=15, current=<file constant>, integer=False)` → cells are `current + k*step` with step = `(hi-lo)/(n-1)` rounded to the nearest `{1, 2, 2.5, 5}×10^k`, covering `[lo, hi]` (ends rounded outward, at most one extra cell each side; `current` always covered). The default `n=15` gives ≈ 11–21 cells after rounding — keep it: two such axes are 100–400 combos, ≤ 10 s for any strategy (scan time is linear in combos × bars; a 40×40 grid on two years of 5-min bars takes ~45 s), a finer step has no trading meaning, and the 3×3 neighbourhood mean degenerates into a single-cell pick when neighbours differ by noise only. Rule: **range from the distribution (p5–p95) or the current value ± a span; cells must be integers or multiples of 1/2/2.5/5; the strategy's current value must be a cell.** Why: the web 穩健參數 tab locates `current` on the axis to mark "you are here" — a percentile linspace (`0.065, 0.543, 1.022…`) never contains the file's constant, so the tab shows 「不在掃描範圍」 and cannot highlight the current cell. `integer=True` for bar counts (SMA period, hold bars): step ≥ 1, int cells. An axis longer than 40 is coarsened (next nice step), never truncated. `step=` overrides the step (still anchored on `current`).
- `from lib.param_scan import percentile_thresholds` — momentum threshold pair: `percentile_thresholds(series, n_parts=17, current=(s.ENTRY_TH, s.EXIT_TH))` → `(entry_vals, exit_vals)` on one nice step across p5–p95 (default 17 cells across the whole span → each half ≈ 9 cells, ~100 combos), entry covering the upper half, exit the lower half, each anchored on its current constant (**always pass `current=`**; omitted → both anchored at 0). Prints the distribution and both axes.
- `from lib.param_scan import scan_grid` — run 2D param scan, returns Sharpe grid. Supports all strategy types:
  - Cells whose params never produce a trade are left NaN (excluded) — a do-nothing cell's Sharpe 0.0 would otherwise win the plateau in a losing period. If `find_plateau` raises "every grid cell is NaN", no combo traded: widen the scan ranges toward the indicator's actual value range.
  - `row_param`/`col_param`: kwarg names forwarded to `compute_signals_fn` (default `'entry_th'`/`'exit_th'`)
  - `warmup`: leading bars to skip from PnL (rolling window warm-up); `compute_signals_fn` receives the FULL df for accurate rolling, PnL computed on `df.iloc[warmup:]`
  - `valid_fn`: `(row_val, col_val) → bool` to skip invalid combos (default: `row > col`; for SMA use `lambda f, sl: f < sl`)
  - If `compute_signals_fn` returns a tuple `(signal, settle)`, `settle` is automatically used as `exec_shifted`
  - **All strategy types use the same `scan_grid` call** — see `examples/btc_sma_cross/scan.py` for the canonical SMA-scan pattern and `examples/btc_ti_5min/scan.py` for the threshold-scan pattern
- `from lib.param_scan import find_plateau, write_scan, plot_heatmap` — plateau detection, scan.json, heatmap chart
  - `find_plateau` returns 5 values: `best_idx, nbr_mean, best_row, best_col, best_sharpe` — use `best_row`, `best_col`, `best_sharpe` directly; `nbr_mean` is a **2D array** (do NOT format it as a scalar). **Keep `nbr_mean` — `write_scan` needs it.**
  - Canonical usage: `best_idx, nbr_mean, best_row, best_col, best_sharpe = find_plateau(grid, ROW_VALS, COL_VALS)`
  - `from lib.param_scan import on_edge, extend_axis` — **plateau on a grid border → extend that side once and rescan.** `on_edge(best_idx, grid.shape)` → list of `(axis, side)` (`axis` 0 = rows / 1 = cols, `side` `'lo'` / `'hi'`; empty when interior, two entries at a corner). `extend_axis(vals, side, k=5, floor=None)` → the same axis with `k` cells added beyond that end at the axis's own step (int axis stays int; clipped so the axis never exceeds 40 — at 40 it comes back unchanged; `floor=1` for bar counts so a `'lo'` extension never yields a 0/negative window). A border plateau was measured on a truncated neighbourhood, so after `find_plateau`: `for axis, side in on_edge(best_idx, grid.shape)` → `extend_axis` the matching axis → run `scan_grid → find_plateau` again on the extended axes (a plain rescan is simpler than merging grids) → `write_scan` with the extended axes. **Extend once only** — the rescan is part of the same scan and the same iteration under the Iteration Brakes; if the plateau is still on the border after that, report it as such and stop. Pattern: `examples/btc_ti_5min/scan.py`, `examples/btc_sma_cross/scan.py`.
  - `write_scan(grid, row_vals, col_vals, nbr_mean, best_idx, output_dir, row_param, col_param, fee, start, end, current=None, window=1)` → writes `strategies/{strategy_name}/scan.json` (atomic). **Mandatory after every scan — the web 穩健參數 tab is built from this file; a scan that skips it is invisible on the web.** `row_param`/`col_param` are the strategy **constant names** (`'ENTRY_TH'`, not the `entry_th` kwarg — the web writes "把 ENTRY_TH 改成 …" from them); `current=(s.ENTRY_TH, s.EXIT_TH)` is what `strategy.py` holds right now (the web marks it; off-grid values are kept with `i/j = null`); `end` = the scan data's last date (`df.index[-1].strftime('%Y-%m-%d')`). It records both the global **peak** (nanargmax of `grid`) and the **plateau** (`best_idx`, Sharpe = neighbourhood mean) — report both, recommend the plateau. **Each axis is capped at 40 values** (api limit — `write_scan` raises past it; a bigger grid would upload nothing and leave the web tab blank), so keep scans within 40×40 via a coarser step or narrower range. An all-NaN grid never yields a scan.json (`find_plateau` raises first) — widen the ranges and rescan rather than writing anything by hand.
  - `plot_heatmap` `output_path` is **required** — always pass `output_path='strategies/{strategy_name}/heatmap.png'`, never `/tmp/`
  - `plot_heatmap` **auto-sends the heatmap to Telegram** (via `send_photo`) — you do NOT need a manual `send_photo` after it. Pass `send_telegram=False` only if you explicitly want to suppress it.
- `from lib.analysis import precise_pnl, compute_stats` — available if you need a custom loop (rare)

**Parameter scan workflow** — fixed order, one `scan.py` in the strategy folder: `scan_grid → find_plateau → (on_edge → extend_axis → scan_grid → find_plateau, once) → write_scan → plot_heatmap`.
Grid-size principles (Pardo's plateau-search practice / industry convention): (a) **10–20 values per axis** — `nice_grid` / `percentile_thresholds` defaults do this (100–400 combos; ≤ 10 s when `compute_signals` is vectorized — a per-bar Python loop costs ~0.5 s per cell on 390k 5-min bars; a two-sided threshold strategy built on `lib.strategy.threshold_position` scans each side with the other side pushed out of range (±1e9), which the lib turns into the vectorized one-sided path; `references/strategy-code.md` › *Scanning four thresholds*); (b) **the step must mean something in trading terms** (whole bars for windows, a threshold move a trader would actually notice) — a finer step only makes neighbouring cells differ by noise and the plateau's neighbourhood mean collapse into a single cell; (c) **coarse first, then fine** — if the plateau needs resolving, a second scan zooms into its neighbourhood with the same rules rather than starting fine. 40 per axis stays the hard cap (api limit).
1. Run `scan.py`: pick the ranges — look at the distribution first (p5–p95, or the current constant ± a span), then build the axes with `nice_grid` / `percentile_thresholds` at their default sizes, **anchored on the constants now in `strategy.py`** (cells = integers or multiples of 1/2/2.5/5, current value on the grid — otherwise the web tab says 「不在掃描範圍」 and cannot mark the current cell; see `references/strategy-code.md`), `scan_grid`, `find_plateau`; **if `on_edge(best_idx, grid.shape)` is non-empty, `extend_axis` that side 5 cells and rerun `scan_grid → find_plateau` once** (same iteration; never a second extension); then **`write_scan`** (pass the same `current=` and the axes actually scanned), `plot_heatmap`. Three or more constants → pairwise coordinate descent, max two rounds, one `scan.json` (`references/strategy-code.md` › *Scanning three or more parameters*; example `examples/tw2317_broker_zscore/scan.py`). One real constant only → scan it 1D (a `range`/loop over `compute_signals`, PNG only) and write no `scan.json` — the web draws 2D grids only; never pad with a fake axis. Report BOTH the peak (highest single cell) and the plateau (best neighbourhood mean) with their Sharpes, and recommend the plateau — a lone spike is the overfit cell.
2. **Update the params directly in the existing `strategy.py`** — do NOT create a new strategy folder
3. Run backtest in the same `strategy.py` to verify
Never create a duplicate strategy folder just because you ran a scan.

The web workspace sends three fixed prompts (bilingual — the zh or en version depending on the user's UI language; recognise either); treat them exactly as follows:
- **Scan** — zh「請掃描策略 {name} 的參數（lib.param_scan…」/ en「Please scan the parameters of strategy {name} …」 → run step 1 above on the strategy's two main constants and reply with the peak and the plateau (param values + Sharpe). This is explicit permission for **one** scan and counts as **one** iteration under the Iteration Brakes — do NOT go on to edit `strategy.py` or re-run the backtest; the user picks on the web. **Do NOT run MCPT on a scan** — neither per cell nor on the plateau: a scan is dozens to hundreds of backtests and MCPT on top would make it unusably slow (`scan_grid` never calls it by design). The p-value comes from the backtest that follows the adopt step.
- **Adopt** — 「請把策略 {name} 的參數改成 A=…、B=…(穩健參數),重跑回測 … 不用再確認」(en equivalent ends the same way: no further confirmation) → the user has already chosen: set the named constants in `strategies/X/strategy.py` to exactly those values, run the backtest once, report the new stats. No confirmation question, no counter-proposal, no extra scan.
- **Walk-forward (樣本外驗證)** — zh「對策略 {name} 做 walk-forward 樣本外驗證:訓練窗 {lookback} 天、每 {step} 天重新最佳化 …」/ en「Please run a walk-forward out-of-sample validation on strategy {name}: training window {lookback} days, re-optimize every {step} days …」 → run `lib.walk_forward.run_walk_forward` on the strategy's two main constants with the `lookback` / `step` the prompt names, on the SAME axes a scan would use, and reply with the out-of-sample Sharpe, the in-sample average and the WFE. `run_walk_forward` **is** the implementation of what that prompt describes — never hand-roll a per-run scan loop (n full scans instead of one), never run MCPT here (this tab shows no p-value), and never edit `strategy.py` afterwards: a walk-forward produces NO adoptable parameters, its result belongs to no single fixed pair. One walk-forward = **one** iteration under the Iteration Brakes.

`lib/walk_forward.py`:
- `from lib.walk_forward import run_walk_forward, default_windows` — **walk-forward out-of-sample validation**: re-pick the parameters inside each training window, apply them to the test window that follows, stitch the test windows into one out-of-sample series. Writes `strategies/{name}/wf.json` (atomic) — the web 樣本外驗證 tab is built from this file, so a validation that skips it is invisible on the web. **Rolling only** (the training window slides forward one step per run); anchored windows are not offered.
- `run_walk_forward(data, compute_signals_fn, row_vals, col_vals, output_dir, row_param, col_param, fee=..., row_kw='entry_th', col_kw='exit_th', lookback_days=None, step_days=None, valid_fn=None, warmup=0, window=1, current=None)` — `data` / `compute_signals_fn` / `valid_fn` / `warmup` are exactly `scan_grid`'s (Type A **and** Type C, `exec_at_close` and the TXF settlement mask travel the same path); `row_param` / `col_param` are the strategy **constant** names as in `write_scan` (`'ENTRY_TH'` — the web compares them with the code to tell a stale result) while `row_kw` / `col_kw` are the `compute_signals` kwarg names; `current=(s.ENTRY_TH, s.EXIT_TH)` is what the file holds now. Axes come from `nice_grid` / `percentile_thresholds` exactly as for a scan and are **fixed for the whole validation** — no per-run `on_edge` / `extend_axis`, every run must land on the same lattice or the drift grid has no axis to plot against. Max 40 per axis (api cap).
- `default_windows(total_days)` → `(lookback_days, step_days)`, three tiers: **1095 / 30** (three-year training window, re-optimised monthly) when the history is ≥ 1185 days; **365 / 30** at 455–1184 days (so a mid-length history never gets a window shorter than a year from the ratio formula); shorter histories fall back to `step = max(30, floor(total/12))`, `lookback = 4 × step` (≈ 8 runs; **floor, never round** — at 730 days `round` gives 61 → only 7 runs). The step stays 30 on every tier: the run count only sets how often the pick is redone; the out-of-sample span is total days minus the training window. Pass the user's numbers when the prompt names them; omit both to take this default. Run count is bounded at 1000 (api cap), not a workload concern — 44 runs (365 / 30) on 4.7 years of hourly BTC took under two minutes end to end.
- **Single-constant walk-forward** (a strategy with ONE tunable constant): the grid is 2-D by contract, so run it 1-D through a one-cell second axis — `row_vals = nice_grid(…, current=s.X, integer=…)`, `row_param='X'`, `row_kw='x'`, `col_vals=[0]`, `col_param='_'`, `col_kw='_none'`, `compute_signals_fn=lambda df, x, _none=0: s.compute_signals(df, x=x)` (swallow the unused kwarg), `valid_fn=lambda r, c: True`, `current=(s.X, 0)`. The web reads `col_param '_'` as "no second axis" and draws a one-column drift grid. Never pad the second axis with a fake range. Verified end to end (lib → api → web) 2026-09-11.
- **Refusals** (both verified to be needed on a real machine): (1) 「把樣本外驗證選中的參數套進策略」→ 「樣本外驗證不產生可採用的參數——它的成績來自每輪重選,不屬於任何一組;要換參數請走參數掃描的穩健點。」 then stop, no edit, no rerun. (2) 「改用 anchored」→ 「這裡只提供 rolling(訓練窗跟著往前滑);anchored 不提供。」 then run rolling, or stop if declined. Do NOT implement anchored by editing `lib/walk_forward.py` — it is read-only (AGENTS.md › Shared Library; the resident runtime refuses the edit tools on it), and `wf.json` has no field to mark an anchored run, so the web would display it as rolling.
- **Cost**: two passes, together about 1.2 × one parameter scan. Pass 1 computes each combo's signals ONCE on the full history and prices every run's training slice from that single pass (a runs × rows × cols Sharpe grid); pass 2 recomputes only the run winners (≤ n_runs combos, usually fewer). No 400-combo × full-history position matrix is ever held — that would not fit a 2C/4GB Starter.
- **Three semantics that are contract, not implementation detail** (also in the module docstring):
  1. **Picking uses the neighbourhood mean; reporting uses the cell itself.** Each run picks with `find_plateau` (same 3×3 rule as the scan), but the reported `train_sharpe` — and therefore `is_stats['Sharpe Ratio']`, the WFE denominator — is the picked cell's OWN Sharpe. A neighbourhood mean is structurally lower than its centre cell, so using it as the denominator would inflate WFE and make the whole tab read optimistic.
  2. **A seam carries the incoming run's own position.** When run k picks different parameters from run k−1, the first bar of run k's test window holds whatever run k's parameters say to hold — the series is cut, not reset. That is what actually happens when you switch parameters; forcing every seam flat would invent n−1 exits nobody traded. The switching cost is charged automatically (the stitched series changes weight at the seam and `precise_pnl` bills the fee). Every per-run test number is sliced out of that ONE stitched series, never re-priced per run, so the run table's trade counts sum to the stitched total and its returns compound to the curve's end.
  3. **Warmup is applied to every training slice.** Signals are computed on the full history, but each training window's PnL skips that window's first `warmup` bars — otherwise run 1 trains on bars where the indicator has not warmed up and picks noise, and the runs would not be the same effective length. Test windows are not trimmed (already warm, and they must stay contiguous to stitch). This is also why no embargo is needed: the test window sits immediately after the training window it was picked from. **Pass the strategy's `WARMUP`.**
- **Guards** (all refuse rather than write something misleading): fewer than 3 runs raises and writes nothing (「資料只夠 K 輪」); more than 60 runs raises (use a longer step); a run whose training Sharpe is ≤ 0 is marked failed — it stays in `runs[]` and in `excluded_runs`, but is left out of the in-sample average; an in-sample average below 0.25 writes `wfe: null` instead of dividing (a tiny or negative denominator inverts the diagnosis).
- **No MCPT, ever** — this tab shows no p-value, and `lib/validation.mcpt` permutes returns at a fixed position, which answers a different question. The p-value stays in the backtest tab.
- `wf.json` contract (shared with api/web — do NOT rename keys): `row_param`, `col_param`, `row_vals`, `col_vals`, `current` (the two constants as VALUES, or null), `lookback_days`, `step_days`, `n_runs`, `tail_days` (days at the end that were too few for one more run), `fee`, `start`, `end`, `oos {dates, cum}` (one point per calendar day; `cum` = cumulative return in **percent**), `oos_stats` / `is_stats` (the flat `'Sharpe Ratio'` / `'Ann. Return [%]'` / `'Max Drawdown [%]'` / `'Trades'` keys `lib/runner.py` already uses — `is_stats` values are the average over the runs that were NOT excluded, and are null when every run failed), `wfe` (number or null), `excluded_runs` (run numbers, 1-based), `runs[]` (`{k, train_start, train_end, test_start, test_end, params, train_sharpe, test_sharpe, test_return, trades}`; `params` are the axis values, `train_start` is post-warmup, `test_return` is that window's period return in percent, not annualized), `generated_at`. The api validates the shape and drops the WHOLE block if anything fails — never hand-write this file.
- **Walk-forward workflow** — one `wf.py` in the strategy folder (never inside `strategy.py`): build the axes the same way `scan.py` does → `run_walk_forward` → report the numbers. It counts as ONE iteration under the Iteration Brakes, and it does **not** end with adopting parameters: the result is a verdict on "picking parameters from history", not a parameter set. Adopting still happens only through the parameter-scan route.

`lib/validation.py`:
- **MCPT is automatic in every Type A backtest.** `run()` calls `mcpt()` on the backtest's own close / position (`fee=FEE`, `periods_per_year` from the data's real date span — the same figure the Sharpe is annualized with — `vol_window` = one month of bars (`max(20, round(periods_per_year/12))`: 1h crypto → 730 ≈ the lib default 720, daily → 21; the manual daily-stock recipe below uses 60), `max_lev` / `target_vol` at lib defaults, permutations drawn from a private RNG seeded 42 so the same backtest always gives the same p-value without touching the global numpy seed) and writes three flat keys into `stats.json` next to the other stats: `"MCPT p-value"` (float), `"MCPT Permutations"` (int), `"MCPT Distribution"` (`{"edges": [31 floats], "counts": [30 ints], "actual": float}` — the permuted-Sharpe histogram the web draws; `actual` is the strategy's own MCPT Sharpe and may sit outside `edges`). The backtest stdout shows `MCPT p-value: … (n=…)` — report it with the other stats. Strategy constants: `MCPT_N = <int>` overrides the permutation count (default 2000, ≈ a few seconds on 40k bars); `MCPT = False` skips the test. The count actually run is scaled to a runtime budget of 4e8 bars × permutations (`n_eff = min(MCPT_N, max(200, 4e8 // bars))`, hard cap 20000): 200k bars (5min, 2 years) still get 2000, 1M bars (1min, 2 years) get 400 — a reduction is logged as a warning naming both counts and `"MCPT Permutations"` shows the effective n. If the histogram comes out unusable (edges collapsed) but the p-value is sound, p-value + permutations are written without the Distribution. A strategy with 0 trades, too-short data or any mcpt failure just logs a warning and writes no MCPT key — never a failed backtest. **Type C (portfolio) has no automatic MCPT** — `mcpt()` tests one price series against one position series, and collapsing a portfolio into one return series would be the forbidden bootstrap; don't fake one.
- **The parameter scan never runs MCPT** (`scan_grid` goes straight through `precise_pnl`; a scan is dozens to hundreds of backtests). Do not add it to `scan.py` or run it on the plateau — the adopt step's backtest produces the p-value.
- `from lib.validation import mcpt, plot_mcpt, write_mcpt_to_stats` — manual use only, for a **different permutation count** or the `mcpt.png` chart: `mcpt(close, position, n=..., fee=..., target_vol=..., ...)` → `(actual_sharpe, p_value, dist)`, then `write_mcpt_to_stats(STRATEGY_NAME, p_value, len(dist), dist, actual)` — merges the same three keys into the existing `strategies/<name>/stats.json` (atomic, other fields untouched; without `dist, actual` it writes only p-value + permutations and removes a stale Distribution), then `plot_mcpt`. Requires a `stats.json` from a prior backtest (raises otherwise — never fabricate one). Lifetime: an explicit **backtest** rebuilds `stats.json` from scratch and recomputes the MCPT keys itself (a manual rerun is overwritten — rerun it after the backtest if you need a specific `n`); a **live / cron tick** rewrites `stats.json` too but carries the three keys forward (same code, same parameters, p-value still valid), so a deployed strategy keeps showing it. The same rule stamps `"Generated At"` (epoch int) on every explicit backtest and carries it through live ticks — the web compares it with `scan.json`'s `generated_at` to know whether `scan.current` is still the file's real constants.
- **Any manual validation goes in its own file in the strategy folder** — never inside `strategy.py`: an MCPT rerun or a bespoke Monte Carlo in `validate.py`, a walk-forward in `wf.py` through `lib/walk_forward.py` (see above — do not hand-roll one).
- Daily stock params for a manual run: `periods_per_year=252`, `vol_window=60`, `max_lev=1.0` (the automatic backtest MCPT uses a 30-day window — `vol_window=21` on daily bars — so a manual rerun with these values is not expected to reproduce its p-value exactly). Any `mcpt()` call takes `rng=np.random.default_rng(<seed>)` for a reproducible result
- **Any *other* Monte Carlo / bootstrap has no lib function, and its numbers are only comparable within one calibration.** `lib/validation.py` is MCPT and nothing else; a block-bootstrap worst-case drawdown, a path simulation, a scenario resample are each hand-written in the strategy's `validate.py`. None of them is an MCPT substitute — they yield no p-value, and only a `run()`-based Type A backtest produces one — and the Type C ban above still holds: a portfolio may not be collapsed into one return series to bootstrap it. **Report a percentile, not the extreme.** The worst of N paths is an extreme that gets monotonically worse as `paths` grows, so two "worst drawdowns" drawn with different `paths` are not the same estimator — quote the 5th percentile and the median, and keep the extreme as a footnote. Every such number, wherever it is written down (a note file, a report block, a reply to the user), carries next to it the **calibration fields**, which decide whether two numbers are comparable: signal version (the strategy file's date/mtime, or a revision you name in the note — the workspace is usually not a git repo, so do not count on a commit hash), symbol and data range (start–end), bar interval, resampling scheme (overlapping or not, with or without replacement, fixed or random block length) and block length in bars and the days they equal where applicable, simulated path length, `paths`, target vol and leverage cap, and fee — plus the **reproducibility field** `seed`, which does not affect comparability. The script prints all of them with the number so the two cannot drift apart; a number found without its calibration is unusable — recover the calibration from the script that produced it, or rerun it, but never quote it. Numbers from different calibrations measure different things: never treat them as the same figure — not as each other's baseline, not carried into another table, not mixed in one column. A deliberate side-by-side is a sensitivity study: label it as one and vary a single calibration axis at a time (bar interval alone can move a worst-case drawdown by tens of percentage points).

`lib/quality_check.py` — CLI, not an import: `python3 lib/quality_check.py strategies/<file>.py`. Static scan for a broken/unfilled `compute_signals()` contract (CRITICAL, exit 2), a TAIFEX futures strategy missing `txf_settlement_mask` (CRITICAL, exit 2 — also enforced at backtest time by `run()`), `FEE=0` / non-constant `FEE` (WARNING, exit 1), and an indicator-driven Type A strategy (`_add_indicators`, `rolling`/`ewm`, or a non-price `fetch_*` feed) with no `PLOT_SERIES` declaration (WARNING, exit 1 — `run()` also prints the same hint after a backtest, without blocking; pure price rules are not flagged). Known blind spot: the TAIFEX check triggers on any `fetch_twfutures_ohlcv(_batch)` call, so a strategy that uses the TXF continuous series purely as an exogenous indicator while trading another instrument is also blocked — either add the mask anyway (harmless, it only zeroes signals) or switch to a non-triggering helper endpoint such as `fetch_twfutures_pcr`. Run on any Type A/C strategy before its first backtest and before marketplace submission (full flow: `references/marketplace.md`). Companion to `lib/security_check.py` (malicious-code scan for downloaded strategies — same exit-code convention).

`lib/notify.py`:

`$BLAVECLAW_HOME` resolution (strategies are run with `workdir=$BLAVECLAW_HOME/workspace`): when the env var is unset it defaults to `/root/.openclaw` (old BlaveClaw runtime) or `/opt/blave-agent` (Blave Agent runtime — detected by whether `/opt/blave-agent/openclaw.json` exists, not just the directory) on Linux, and `C:\openclaw` on Windows — the same resolution `lib/notify.py` itself uses. Getting it wrong does not error: notify just silently stops sending Telegram alerts, so check the real environment rather than assume.
- `from lib.notify import make_sender, send_text, send_photo`
- `make_sender()` → text sender function (broadcasts to all paired chat IDs)
- `make_sender(photo=True)` → photo sender function
- Senders raise `RuntimeError` if Telegram rejects the send (never fails silently) — do not report a notification as sent unless the call returned without raising
- Text messages longer than 4096 chars are split into multiple messages automatically (`TELEGRAM_TEXT_LIMIT`)
- Use `send_telegram_fn=make_sender()` when calling `run()`
- **CRITICAL — Pairing check (run at session start, before any other action):** Check pairing status first (see Telegram Pairing Check in `references/strategy-code.md`). If not paired: tell the user "Telegram is not paired yet. Please complete the pairing flow via the bot." Do not proceed with any strategy run or notification until pairing is confirmed.

`lib/strategy.py`:
- `from lib.strategy import add_realized_vol` — computes realized_vol in-place. **Standard window is 30 days** — convert to bars based on strategy interval (e.g. 1d→30, 1h→720, 5min→8640)
- `from lib.strategy import apply_vol_scaling` — scales signal by `(target_vol / realized_vol).clip(vol_cap)`; works for both long and short; call at the end of `compute_signals`
  - Standard defaults: `target_vol=0.30`, `vol_cap=2.0`
  - Must call `add_realized_vol` first so df has a `realized_vol` column
- **All risk-parity strategies must use these two functions — do NOT compute vol inline in the strategy file**
- `from lib.strategy import hysteresis` — `hysteresis(x, enter, exit, side=+1)` → position Series, vectorized one-sided threshold hysteresis: long once `x > enter`, flat once `x < exit`, held in between (`side=-1`: short once `x < enter`, flat once `x > exit`); NaN holds, leading bars flat. The building block for a one-sided threshold strategy
- `from lib.strategy import threshold_position` — `threshold_position(x, buy_th, sell_th, cover_th, short_th)` → 1 / 0 / -1 Series, the two-sided four-threshold state machine (exit before entry, NaN holds). A Python loop (~0.5 s per 390k bars) — but when one side's entry is unreachable (a scan's ±1e9) it returns the vectorized `hysteresis` of the other side, so per-side scans stay fast
- `from lib.strategy import list_versions, restore` — strategy **versions** (one per backtest, `strategies/<name>/versions/`): `list_versions(name)` → the summary entries newest last (`n`/`at`/`note`/`ret`/`sharpe`/`sortino`/`mdd`/`trades`/`mcpt_p`/`start`/`end`/`code_hash`, `[]` when never versioned) — answer 「哪一版 Sharpe 最好」 from it rather than opening the per-version blobs; `restore(name, n)` puts that version's code back into `strategy.py` and **raises** when the strategy is funded (`amounts > 0` → fork-and-switch instead). Minting, the `VERSION_NOTE` constant and the web's fixed restore prompt: `references/strategy-code.md` › *Versions*

`lib/pnl.py`:
- `from lib.pnl import daily_returns_typeA, daily_returns_typeC` — extracts daily returns from pf_series (called automatically by runner, no manual use needed)
- `from lib.pnl import load_all_stats` — reads all `strategies/*/stats.json` (including daily_returns) for use by manager

`lib/report.py`:
- `from lib.report import write_report, status` — writes a workspace report into the drop directory `workspace/reports/<id>.json` (atomic write, envelope + leading `meta` block filled in) and reports where it got to (`pending` / `sent` / `failed: <reason>` / `unknown`)
- `write_report(report_id, title, blocks, type="research", report_type=None, created_at=None, meta=None, images=None)` → path. `images={"perm.png": <bytes>}` writes the picture sidecar `reports/<id>.files/` before the JSON, for `image` blocks referencing `{"file": "perm.png"}` — the only way to ship a figure from a scheduled run, which has no machine token. Only the id (`[A-Za-z0-9_-]{1,64}`, = the file name, re-use overwrites that report) and the image file names (must be plain names, not paths) are validated here; the api is the only validator and its refusal names the offending field in `reports/upload_errors.log`. For `type="research"` it also prints advisory `WARNING:` lines (title wider than 80, where a CJK character counts 2; no `kpi_row` right after the lead — `references/reports.md` §7b). They never stop the write
- Block types, field limits and the drop-directory contract: `references/reports.md`.

`lib/report_templates.py` — four templates: `tw_market_brief()` 台股大盤晨報, `tw_close_brief()` 台股收盤報告 (`pack.skip` on a non-trading day, and then `publish` writes nothing), `crypto_market_brief()` 加密市場晨報, `symbol_brief(symbol)` 單標的晨報. Usage and narrative rules: `references/reports.md` §1b.

`lib/watch.py`:
- `from lib.watch import add_widget, update_widget, remove_widget, write_data, status` — puts widgets on the user's workspace watchboard by dropping op files in `workspace/watch/ops/` (atomic write; the runtime uploads and moves them to `sent/` / `failed/`) and feeds machine widgets through `workspace/watch/data/<widget_id>.json` (overwritten in place, ≤ 64 KB, picture sidecar `<widget_id>.files/`)
- `add_widget(id, type, title, *, symbol=None, symbols=None, interval=None, block_type=None, refresh_cron=None, refresh_human=None, script=None, w=None, h=None)` → op path. Stream types `price` / `kline` (`interval`) / `book` (TXF, MXF only) / `watchlist` (`symbols` 1–20) take a symbol (stock id, `TAIEX`, `TXF`, `MXF`); the strategy type `strategy_chart` takes `strategy=` alone (one of this machine's own strategies, and it must have trades — the platform draws its entry/exit chart, this machine sends nothing); the machine type `block` takes `block_type` + `refresh_cron` (5 fields, once a minute at most) + `refresh_human` + `script` and also registers `report_jobs/<id>/` (`kind: "watch"`, through `register_schedule`, so the id must be a slug). Everything is validated here and raises `ValueError` before any file is written
- `update_widget(id, *, title=None, props=None, refresh_cron=None, refresh_human=None, script=None)` → op path (None when only the script changed); never the grid — layout is the user's. `remove_widget(id)` → op path, and deletes the job directory, data file and sidecar
- `write_data(widget_id, block, images=None)` → data path; for the scheduled `run.py` only: checks the block type against the declared `block_type`, refuses obviously wrong shapes / NaN / > 64 KB, never touches the network. `status(op_file_or_widget_id)` → `pending` / `sent` / `failed: <reason>` / `unknown` — a diagnostic, never something to poll
- Catalogue, sizes, the script template, failure semantics and worked examples: `references/watchboard.md`

**Exchange account libraries** (`lib/account_{exchange}.py` — equity + positions readers, discovered by filename):
- **Symbol contract:** every `get_positions()` returns `symbol` as canonical dashless uppercase (`BTCUSDT`, never the venue's `BTC-USDT` / `BTC_USDT`) — the reconciler keys on this format. Any strategy code comparing a position's symbol against a venue-format string must normalize BOTH sides first — use `from lib.data import normalize_symbol` (the single recipe: strips `/`, `-`, `_`, uppercases); a raw comparison never matches and silently reads as "no position" (live incident: false position-closed notifications + strategy re-opening on top of an existing position). Spot inventory rows carry an `@spot` suffix on top of the canonical symbol — normalize the symbol part only (see the MARKET/spot keying section below)
- `lib/account_bingx.py` already ships implemented (swap equity + spot/fund wallet breakdown) — do NOT rewrite it, extend it in place
- `lib/account_okx.py` already ships implemented — unified TRADING account (`totalEq`) as equity + separate `funding` wallet in the breakdown; `OKX_DEMO=true` in `.env` routes to OKX simulated trading. Position sizes are ctVal-exact (pos × ctVal from the public instruments endpoint, cached — the positions response's own `ctVal` field is null, and notional/markPx reads back imprecise: measured 0.001 ETH as 0.00099911, which floors to 0 contracts and becomes unclosable dust)
- `lib/account_binance.py` already ships implemented — USDⓈ-M futures `totalMarginBalance` as equity + ALL ten wallets in the breakdown (spot/funding/cross+isolated margin/UM+CM futures/earn/options/bots/copy) via `/sapi/v1/asset/wallet/balance` (BTC-denominated, converted; every row measured accurate on a live account, unlike BingX's overview endpoint)
- `lib/account_gateio.py` already ships implemented — USDT futures account (`total` + `unrealised_pnl`) as equity (the wallet orders draw on); every other wallet (spot/margin/delivery/options/earn…) via `/wallet/total_balance` in the breakdown (needs wallet permission on the key — best-effort, and the transfer endpoint `/wallet/transfers` needs it too). A futures account that was never funded raises `USER_NOT_FOUND` — handled as equity 0 (working link), not an error. Position sizes are quanto_multiplier-exact (signed contracts × multiplier from the public contract detail, cached — the OKX ctVal lesson); dual (hedge) mode rows carry `mode=dual_long/dual_short`. Accepts `GATEIO_*` (web 下單設定) or `GATE_*` (skill convention) key names
- `lib/account_bybit.py` already ships implemented — **Unified Trading Account (UTA) only**; a classic account raises a readable "upgrade and rebind" error rather than half-reading it. A UTA has exactly TWO wallets read through TWO different endpoints: UNIFIED via `/v5/account/wallet-balance` (the only accountType it accepts) and FUND via `/v5/asset/transfer/query-account-coins-balance`. Equity = UNIFIED `totalEquity`; FUND cannot trade and is NOT in it, so it appears in the breakdown only — omit it and the user's money looks like it vanished. No separate spot wallet: spot inventory IS the UNIFIED coin array. `get_flows` reports withdrawals as `amount + withdrawFee` — Bybit charges the fee on top, and the platform needs the real balance debit. Wallet model, flows-by-endpoint口徑 and the rest: `references/bybit-skill.md`
- `lib/account_paper.py` ships implemented — the simulated **paper** venue (no keys; `PAPER_API_KEY`/`PAPER_SECRET_KEY` are fixed markers the web writes). Equity = cash + unrealized swap PnL + spot inventory, marked via the TRADING STRATEGY's own data source (`lib/paper_data`, not an independent feed); **no `accounts` wallet breakdown** — paper is one simulated account, so the web falls back to the total `equity` (a cash/unrealized/spot split would be wrong as a wallet list and the web sums accounts into equity). positions/holdings read `state/paper_ledger.json` through `order_paper.snapshot()` (which also settles resting limits and SL/TP triggers — reading IS the matching cadence); `get_flows` is always `[]` (the seed is not a deposit)
  - **Paper venue — web handoff:** the web binds paper like any venue (`PAPER_API_KEY`/`PAPER_SECRET_KEY` are fixed markers, not secrets) and fills are simulated at live Binance public prices. On the handoff only run the read-only validation and tell the user how fills are priced. Never hand-write a paper lib — if `lib/order_paper.py` is missing the workspace is behind: update per `references/updating.md` first. `order_paper.reset_account(env)` only on an explicit user request.
- All five also implement `get_holdings(env)` — coin holdings in spot/funding wallets (`[{asset, amount, usdt_value, wallet}]`, display-only): the web lists them under 持幣 next to the positions table. **Holdings are NEVER positions and never reach the reconciler** — a spot coin fed to it would net futures orders against spot inventory. Unpriceable coins (delisted, e.g. ETHW) keep `usdt_value=None` but are still listed; dust <0.1 USDT dropped.
- All four also implement `get_flows(env, since)` — external (on-chain) deposit/withdraw records since `since` (epoch seconds): `[{ts, direction: 'in'/'out', currency, amount, txid}]`, ascending ts, amounts always positive, COMPLETED records only. The platform nets these flows out of equity-snapshot deltas to get PnL. Same-exchange internal transfers (own-wallet, sub-account, user-to-user off-chain) are excluded per venue: Binance `transferType != 0` skipped; OKX filtered server-side with `type=4`; Gate.io and BingX capital endpoints are on-chain-only (Gate.io GateCode 'b'-ids skipped too; BingX internal transfers use a separate Asset-transfer-records endpoint). **BingX does NOT reuse Binance's transferType semantics** — on its capital history transferType is a direction flag (0=deposit, 1=withdrawal), so it is NOT a filter (verified on a live account 2026-08-05: a real on-chain withdrawal came back status=6 transferType=1; the earlier `transferType != 0` guard silently dropped every withdrawal). Status alone selects completed external flows. Query-window limits are handled inside (Binance/BingX slide 89-day windows, Gate.io 29-day — 90/30-day API caps; OKX pages by ts cursor, no cap). `txid` is the chain tx hash, falling back to the venue's record id (BingX deposits have no record id — deterministic synthetic key). Fee semantics differ: Gate.io withdrawal `amount` includes the fee (full balance debit); Binance/OKX/BingX exclude it, so fee-sized residue shows up in PnL as a cost. Any API error raises — callers log it, never swallow. Incremental callers should overlap pull windows (a record still pending at one pull completes later at its ORIGINAL ts — re-pull the last day or two and dedup by txid). Two items still owe a real-account check (doc verification alone could not close them): OKX — send one internal transfer and confirm `type=4` really keeps it out of both history endpoints; BingX — after a deposit completes, confirm which status value it actually lands on (official reference says 1=Not credited while ccxt's executed mapping treats 1 as completed; the ccxt reading {1, 6} is what ships).
- For any other exchange, copy `lib/account_TEMPLATE.py` — see `references/manager.md` § Account library

`lib/venue_wiring.py` — **reconciler auto-wiring for official venues. The template `manager/reconciler.py` already routes through it — when the bound venue ships both official libs (Binance/BingX/OKX/Gate.io), there is NOTHING to wire, and rebinding to another official venue needs no reconciler change.** Detects the venue from `.env` keys + lib files, maps the reconciler's USD-diff contract onto the venue's order lib (swap via `get_mark_price`, spot via quote-sized buys / inventory-capped sells, `spot_scope` incl. exit-on-removal), and bakes in the harvested fixes (reduce legs ceiled to a whole lot and capped at the position, entry legs rounded half-up to a whole lot — paired with `manager/reconciler._symbol_threshold`'s per-symbol gates, entry at the venue minimum and reduce at half a lot, so neither rounding is churned back by the other; the older bingx below-min ValueError mapped to the False-skip contract). Hand-wire the two reconciler functions ONLY for venues without official libs — the template docstrings carry the contract.

`lib/venue.py` — **chat-side venue bind.** `bind(venue_id, env)` hands a key the user pasted in chat to the platform runtime's own `.env` writer (the one the web bind uses: flock, atomic 0600 write, other-venue eviction + halt, `manager/credentials.ui.json`), so the machine ends up exactly as after a web bind. `env` = exactly that venue's `{ID}_API_KEY` / `{ID}_SECRET_KEY` (+ `{ID}_PASSPHRASE` for okx); ids `binance` / `bingx` / `okx` / `gateio` only — Taiwan brokers and paper are refused with a pointer to their own path. Returns `{venue, written (env names), bound, evicted}` — never a value. Never write `.env` by hand instead: a hand-written pair is invisible to `lib/venue_wiring` and skips eviction.

`lib/guard.py` — **kill switch + order audit log.** Enforced inside `lib/order_*.py`'s transport layer and again in `lib/portfolio.reconcile()` — no caller opts in, nothing to wire:
- If the file `state/HALT` exists, every ENTRY order raises `guard.Halted` before any network call. Reduce-only closes, SL/TP, and cancels still work (flattening must never be trapped). The file's existence is authoritative — malformed content still halts.
- `reconcile()` denies exposure-adding legs itself, before calling `place_order_fn`. That is the layer that covers a reconciler whose exchange has no official `lib/order_*.py` and whose `place_order` was hand-written — the transport-layer check alone would miss it. Denials are audited and logged, but deliberately NOT sent to Telegram: the user tripped the halt to stop the noise.
- `trip_halt(reason, source)` — set it (user says 停 / healthcheck anomaly). `clear_halt(source)` — **only on explicit user instruction; NEVER clear a halt on your own initiative.** `halted()` / `halt_info()` — check state.
- Every order attempt / outcome / denial is appended to `state/audit.jsonl` (fsynced). When the user asks "你到底下了什麼單", read this file — it is the record of what was actually sent, not what was intended.

`lib/order_TEMPLATE.py` — **contract for writing a new perp venue's order lib** (Taiwan brokers follow `lib/order_sinopac.py` instead). Copy it, keep the filename convention (`lib/order_{exchange}.py`). The reconciler calls four FIXED names — `get_contract_rules` / `format_qty` / `place_market_order` / `close_position_partial` — a lib missing any of them crashes at reconcile time, mid-trade. `lib/order_bingx.py` is the reference implementation.

`lib/order_bingx.py` — **BingX swap (USDT-M perp) order execution. Ships implemented — NEVER hand-write BingX order calls in a strategy; import from here.** All functions take `env` (dotenv dict) first. `direction` is always the POSITION's direction (`'long'`/`'short'`); position mode (one-way vs hedge) is auto-detected.
- `open_position(env, symbol, direction, qty, sl_price=, tp_price=, client_order_id=)` — **the recommended entry flow**: ONE atomic market order with SL/TP attached (no naked window), polled to FILLED, protection verified in open orders. Returns `{'entry': confirmed, 'protection': [...]}`. Raises `ProtectionFailed` if protection isn't visible after the fill — treat that as an ALERT-THE-USER-NOW event, never swallow it.
- `place_market_order(...)` — confirmed market order; returns exchange-reported `avg_price` / `executed_qty` / `commission` (never report the intent — report these)
- `place_limit_order(...)` — returns unconfirmed order with `order_id`; track via `confirm_order`
- `confirm_order(env, symbol, order_id, timeout=15)` — polls to terminal state; raises `BingXError` on CANCELED/FAILED, `OrderNotConfirmed` on timeout (order may still fill — re-query, do NOT blindly resubmit)
- `place_protective_orders(env, symbol, direction, qty=None, sl_price=, tp_price=)` — standalone SL/TP for an existing position; `qty=None` protects the whole position via `closePosition`; verified on-exchange, raises `ProtectionFailed` otherwise. Use for multi-level TPs or repairing protection.
- `close_position(env, symbol, direction, qty)` — confirmed reduce-only market close
- `get_order` / `get_open_orders` / `cancel_order` / `cancel_all_orders`
- `get_fills(env, symbol, start_ms, end_ms)` — fill history (≤30 days; handles BingX's startTs/endTs + fill_orders quirks)
- `format_qty` / `format_price` — precision flooring + min-qty/min-notional validation (raises instead of sending a doomed order). Sizing to a whole lot happens upstream in `lib/venue_wiring.py` (`_entry_qty` rounds, `_reduce_qty` ceils+caps) — this floor is the backstop, see `references/manager.md` › *Order library*
- `get_leverage` / `set_leverage`, `get_position_mode`, `claim_demo_funds` (VST)
- Set `BINGX_DEMO=true` in `.env` to run the same code against VST paper trading
- **Spot layer** (strategies declaring `MARKET = "spot"`): `place_spot_market_order(env, symbol, 'buy'|'sell', base_qty=, quote_qty=, client_order_id=)` — BUYs sized in QUOTE currency (`quoteOrderQty`), SELLs in base qty floored to the spot step (`get_spot_rules`/`format_spot_qty` — spot rules differ from swap: sizes not digit counts, and minimums are ASYMMETRIC: BTC sell-min ≈ $12 vs buy-min $0.5, a small buy can be unsellable dust → returns False); polls to terminal (placement can report PENDING); guard buy=entry / sell=reduce; spot client-id param is `newClientOrderId` (swap uses `clientOrderID`). Plus `get_spot_balances` / `get_spot_price` / `get_spot_order` for reconciler wirings.
- Always pass `client_order_id` (alphanumeric, ≤40 chars, unique per signal — e.g. `f"{strategy}{signal_ts:%Y%m%d%H%M%S}"`) so a resubmit is rejected by the exchange instead of doubling the position
- Wire `manager/reconciler.py`'s `get_positions()` / `place_order()` through this lib + `lib/account_bingx.py` for BingX users

`lib/order_binance.py` — **Binance USDⓈ-M futures order execution. Ships implemented — NEVER hand-write Binance order calls in a strategy; import from here.** Same contract and surface as `lib/order_bingx.py` (all functions take `env` first; `direction` is the POSITION's direction; four reconciler names + limit/cancel/protective/leverage helpers; `lib/guard` halt + audit built in). Binance-specific facts, all measured live 2026-08-04:
- Broker attribution is per order: every order carries `newClientOrderId` prefixed `x-52DDFAFN` (built inside the lib — pass a plain ≤26-char `client_order_id` suffix; over-long raises instead of truncating)
- qty is base units natively (contract_value always 1.0 — no conversion anywhere)
- **Conditional orders (SL/TP) live on the separate Algo Order API**: `/fapi/v1/order` rejects `STOP_MARKET` with `-4120`; the algo endpoints use `triggerPrice` (not stopPrice), `clientAlgoId` (broker prefix rides it), return `algoId`/`algoStatus`, and have their own open/cancel calls — `get_open_orders` does NOT show them, use `get_open_algo_orders` / `cancel_algo_order`; `cancel_all_orders` clears both sides
- No atomic SL/TP on entry (unlike BingX): `open_position` = entry then protective orders, verified on-exchange, `ProtectionFailed` if not visible
- hedge mode rejects the `reduceOnly` param (-1106) — the lib expresses closes via positionSide there; one-way verified live, hedge path code-reviewed only (account was one-way)
- `commission` comes from `userTrades` (order query has none, lags fill ~1s — retried) and is NOT always USDT: BNB-discount accounts pay BNB, so read `commission_asset` alongside
- `BINANCE_DEMO=true` routes to the futures testnet (needs separate testnet keys, unlike BingX VST)
- **Spot layer** (strategies declaring `MARKET = "spot"`): `place_spot_market_order(env, symbol, 'buy'|'sell', base_qty=, quote_qty=, client_order_id=)` — BUYs are sized in QUOTE currency (`quote_qty` → Binance `quoteOrderQty`, no price conversion), SELLs in base qty floored to the SPOT step (separate rules from futures — `get_spot_rules`/`format_spot_qty`); response carries fills inline (`FULL`), no polling; broker prefix `x-GBN6HWR2`; below min → `False`. Guard: buy=entry (halt blocks), sell=reduce (never trapped). Spot cannot short — `lib/portfolio` clamps net-negative spot targets to 0, loudly. Plus `get_spot_balances(env)` / `get_spot_price(env, symbol)` for reconciler wirings.

**Market dimension (spot vs swap) in the reconcile pipeline** — `lib/portfolio.py`:
- A strategy declares `MARKET = "spot"` in strategy.py (undeclared = swap). Spot and swap flow through the SAME pipeline, distinguished by the target/actual KEY: swap keys stay the plain canonical symbol (`BTCUSDT`), spot keys carry `@spot` (`BTCUSDT@spot`) — same symbol, different inventory, never netted against each other.
- Helpers: `market_key(symbol, market)` / `split_key(key)` / `strategy_market(name)` / `spot_symbols()` / `spot_scope(inventory_value_fn)`. Reconciler wirings: `get_positions()` adds `market_key(sym,'spot')` rows for every symbol `spot_scope()` returns (inventory value = base balance × price), and `place_order()` routes on `split_key(symbol)[1]`.
- **Exit-on-removal (futures parity)**: `spot_scope` persists managed symbols in `manager/spot_scope.json` — removing a spot strategy keeps its symbol in `actual` until the inventory sells below the reconcile threshold, so removal EXITS the position instead of orphaning coins (measured 2026-08-04: removal before this existed left inventory stranded with no history entry). Personal coins in symbols no strategy targets never enter the scope; a targeted symbol's inventory is ONE pool — same-symbol personal coins are co-managed (incl. the removal sell-down).

`lib/order_okx.py` — **OKX order execution (USDT perp swap + spot). Ships implemented — NEVER hand-write OKX order calls; import from here.** Same contract and surface as the other order libs (four reconciler names + protective/cancel helpers, `lib/guard` built in). OKX facts, all measured live 2026-08-05 on a hedge-mode account:
- Broker attribution is the body field `"tag": "96ee7de3fd4bBCDE"` — injected by the transport on every order POST
- **swap `sz` is CONTRACTS**: base qty ÷ ctVal, computed in Decimal end-to-end (float division under-sizes by a whole lot at exact multiples — measured: 0.01 ETH became 0.009); `format_qty` returns contracts and is a min-gate ONLY
- The real error is `data[].sCode/sMsg` — top-level code/msg often just says "Operation failed"; the lib surfaces the inner layer
- net mode has native `reduceOnly`; hedge (long_short) mode does NOT — closing = `posSide` addressed with the opposite order side (auto-detected from /api/v5/account/config)
- SL/TP live on `/api/v5/trade/order-algo` (`ordType=conditional`, market exec via `slOrdPx=-1`, whole-position via `closeFraction=1`), verified in `orders-algo-pending`, cancelled via `cancel-algos`
- **Spot layer**: BUYs sized in QUOTE currency (`tgtCcy=quote_ccy`, `sz`=USDT), SELLs in base (`tgtCcy=base_ccy`); `tdMode=cash`; guard buy=entry / sell=reduce; 51020 (below minimum) → `False`
- `OKX_DEMO=true` routes via the `x-simulated-trading` header (demo keys)

`lib/order_paper.py` — **Paper (simulated) venue — no exchange, no keys. Ships implemented; same surface as the other order libs (four reconciler names, limit layer, spot layer, `open_position`/`place_protective_orders`, `lib/guard` built in), so the reconciler / dispatch / flatten / web run unchanged.** What is simulated (full list in the module docstring):
- **Price = the trading strategy's OWN `fetch_data`** (via `lib/paper_data.current_price`, resolved symbol→strategy→its `fetch_data`, latest bar Close; short process-TTL cache so per-minute marking of every position isn't a full-history pull each time). NOT an independent public feed — so a strategy's paper fills use the exact prices (incl. adjustments, e.g. TW `fetch_twstock_price_adj`) it computes signals on, and paper covers whatever the strategy trades (crypto / TW stock / TXF). A symbol no bar strategy owns falls back to a generic 1m crypto kline, else raises `PaperNoPrice` (never a made-up price)
- **Fills are IMMEDIATE at that price, no bid/ask, no slippage** (the live-paper convention, e.g. Freqtrade dry-run; beginner-tier fidelity — Webull/TradingView do the same). Fees ARE modelled (taker 0.05% swap / 0.10% spot, maker 0.02%). Resting limits fill at their OWN limit price when the mark crosses it; post-only crossing → `{'status': 'post_only_rejected'}`; resting orders + SL/TP settle **lazily on every call into the lib** (any get_* / account read)
- **One USD account, no FX**: the reconciler sizes qty = USD ÷ price, so PnL = qty × (mark − entry) = notional × return — a unitless return, so currency/contract unit cancel across crypto/TW/TXF (product decision B). Permissive rules (fetch_data is OHLC only — no min-qty/notional/step gating; the reconciler's slice threshold is the floor). Reduce-only caps at the position, never flips, and is NEVER refused; gross notional capped at 10× equity on exposure-adding fills; a position whose price source vanishes marks at entry (never bricks the account) and stays closeable
- Ledger `state/paper_ledger.json` (atomic writes + flock / msvcrt; `PAPER_INITIAL_EQUITY` in `.env` seeds it, default 10,000 USDT; the web writes `PAPER_BOUND_TS` on every bind and a ledger older than the bind is re-seeded, so unbind→rebind is a fresh 10,000); reduce/close legs are never refused by the equity/leverage checks (only exposure-adding fills are); `reset_account(env, cash=None)` wipes and re-seeds — **explicit user request only**; client_order_id is idempotent (resend → the earlier fill), HALT blocks entries and never closes, every attempt lands in `state/audit.jsonl` with `venue: paper`

`lib/order_bybit.py` — **Bybit order execution (USDT linear perp + spot, UTA). Ships implemented — NEVER hand-write Bybit order calls; import from here.** Same contract and surface as the other order libs. Bybit facts, measured live 2026-09-09:
- **SL/TP are native and ATOMIC** — `takeProfit`/`stopLoss`/`tpslMode:"Full"` ride on `/v5/order/create` itself, so `open_position(sl_price=, tp_price=)` leaves no naked window. A flat position auto-cancels them
- **A price formatted as `"0"` is accepted silently and means NO protection** — `float()`-ing a tick of `0.00001` stringifies as `1e-05`, whose decimal count reads as zero; the lib formats every price from the filter's ORIGINAL string via `Decimal`, and `open_position` reads the position back and raises `ProtectionFailed` if protection is missing
- `get_open_orders` **drops rows with a non-empty `stopOrderType`** — Bybit lists the position's own SL/TP as open orders, and an orphan sweep would strip the protection
- **`orderLinkId` dedup is PERMANENT**, not just the unfilled window (a resend after fill returns 110072) — unlike Binance/OKX, a blind resubmit cannot double the position, so connection-error retries carrying an orderLinkId are safe and resolve back to the original order
- **Hedge mode hard-rejects an order with no `positionIdx`** (10001). Position mode is read fresh on every order, never cached — the user can flip it in the app mid-session
- post-only that would cross is ACCEPTED then auto-cancelled with `rejectReason EC_PostOnlyWillTakeLiquidity` (OKX-style, not an error code) → mapped to `{'status': 'post_only_rejected'}`; `PartiallyFilledCanceled` is normalized to `canceled` in-lib (it is terminal but reads like an open partial)
- cancel of an already-gone or never-existing order is retCode 110001 → returns `{'status': 'order_not_found'}` instead of raising
- Market orders split across several fills — the aggregate on the order row (`cumExecQty`/`avgPrice`/`cumExecFee`) is the answer, never one fill
- **Spot layer**: BUYs sized in QUOTE (`marketUnit:"quoteCoin"`, USD straight in), SELLs in base floored to `basePrecision` (spot filters are a different shape from linear — `basePrecision` not `qtyStep`, `minOrderAmt` not `minNotionalValue`). **The buy fee is charged in the BASE coin** (a 6 USDT buy reporting `cumExecQty` 66.3 credits 66.2337), so size sells from the WALLET, not from the buy's `cumExecQty`. Both sides gate on `minOrderAmt` (retCode 170140 below it). Guard: buy = entry (HALT blocks), sell = reduce (never trapped)
- Symbols are already canonical (`BTCUSDT`), qty is base units, `contract_value` is 1 — no ctVal conversion anywhere

`lib/order_gateio.py` — **Gate.io order execution (USDT perp futures + spot). Ships implemented — NEVER hand-write Gate.io order calls; import from here.** Same contract and surface as the other order libs (four reconciler names + protective/cancel helpers, `lib/guard` built in). Gate.io facts, all measured live 2026-08-05 (single AND dual mode, futures + spot):
- Broker attribution is the header `X-Gate-Channel-Id: blave` — injected by the transport on EVERY request; verified landing: fills carry `biz_info "ch:blave"`, price orders `broker "blave"`
- **futures `size` is signed CONTRACTS** (positive=long, negative=short): base qty ÷ `quanto_multiplier`, computed in Decimal (the OKX ctVal pitfall); `format_qty` returns contracts and is a min-gate ONLY; market order = `price "0"` + `tif ioc`
- **`text` custom ids are labels, NOT idempotency — measured: two orders with the identical text both filled** (no dedup at all, not even Binance/OKX's un-filled window) — on `OrderNotConfirmed` always re-query, a blind resubmit doubles the position; for the same reason the transport does NOT auto-retry POSTs on connection errors (a reset after the request went out may have filled — the reconciler re-derives the diff from live positions next round), GET/DELETE stay retryable
- dual (hedge) mode needs no posSide field: `reduce_only` + the order's sign uniquely addresses the side; whole-position close = `size 0` + `close true` (single) / `auto_size close_long|close_short` (dual) — all verified with both sides open simultaneously
- SL/TP live on `/futures/usdt/price_orders` (initial market order + mark-price trigger, `rule` 1=≥ / 2=≤), own id namespace + open/cancel endpoints; futures fees come from `my_trades` (order responses carry none), spot fees ride the order itself
- **Spot layer**: market BUYs are natively QUOTE-sized (`amount` = USDT spend — Gate.io's own semantics), SELLs in base floored to `amount_precision`; mins are explicit per pair (`min_quote_amount` — 3 USDT on BTC_USDT / `min_base_amount`), below → `False`; guard buy=entry / sell=reduce
- **Spot BUY fees are deducted from the received BASE asset** (bought 0.000093 BTC, fee 9.3e-8 BTC): a buy's `executed_qty` is NOT what landed in the wallet — sell-downs must size from `get_spot_balances` (the venue_wiring sell path already caps at held inventory; selling `executed_qty` raises `BALANCE_NOT_ENOUGH`)
- **A futures account does not exist until the first transfer into it** (`USER_NOT_FOUND: please transfer funds first` — `lib/account_gateio` treats it as equity 0, not a broken key; a fresh binding before any transfer is a working link); an untouched contract has no position record (`POSITION_NOT_FOUND` → `get_leverage` returns 0)
- Errors are `{"label", "message"}` — the machine-readable `label` is surfaced first
- Accepts `GATEIO_*` (web 下單設定) or `GATE_*` (skill convention) key names; no testnet/demo routing (futures testnet needs separate registration — minimal live orders instead)

`lib/order_sinopac.py` — **SinoPac (永豐金) Taiwan stock odd-lot order execution (Shioaji). Ships implemented — NEVER hand-write Shioaji order calls; import from here.** All functions take `env` (dotenv dict) first. Harvested from the first live TW-stock deployment, which lost three rounds of orders to silent rejections (see `references/sinopac-broker.md` § Field-Verified Lessons — read it before any SinoPac work). Requires `SINOPAC_LIVE=true` in `.env` for real orders; without it Shioaji runs in simulation mode against a fake account.
- `place_order_sinopac(env, symbol, signed_diff, client_tag=)` — **the reconciler entry point**: signed_diff in TWD (>0 buy, <0 sell), converted to shares at last price and split into ≤999-share odd-lot MKT orders, each polled to acknowledgement. Returns `{'orders', 'filled_qty', 'target_qty', ...}`, or `False` if the diff is under one share (skip).
- `place_odd_lot_order(env, symbol, 'buy'|'sell', shares, client_tag=)` — one confirmed odd-lot order (1-999 shares). Returns exchange-reported `status` / `filled_qty` / `avg_fill_price` / `msg` — report those, never the intent. Raises `SinopacError` on rejection (with the broker's message), `OrderNotConfirmed` if never acknowledged (query again, do NOT blindly resubmit).
- `client_tag`: ≤6 alphanumeric chars (Shioaji `custom_field`), unique per intent — a same-day resubmit with the same tag raises `DuplicateOrder` locally (Shioaji has no exchange-side clientOrderId dedup).
- `get_sinopac_positions(env)` — `{symbol: {'side': 'long', 'size': TWD}}` (reconciler shape, odd-lot-aware via `unit=Share`); `get_account_balance(env)` — settlement balance in TWD, raises on failure.
- Buy orders pass through `lib/guard` (halt + audit) like BingX; sells are never trapped. Login + CA activation happen on first call and fail loudly if the CA certificate isn't activated.
- Futures (TXF) are NOT covered yet — raw-Shioaji pattern with mandatory confirmation is in `references/sinopac-broker.md`; harvest into this lib after the first live futures deployment.

`lib/order_capital.py` — **Capital (群益) order execution (台指期 + 台股) — SKCOM COM API, Windows only. Ships implemented — NEVER hand-write SKCOM order calls; import from here.** Taiwan-broker shape (follows `lib/order_sinopac.py`, not the perp template); all functions take `env` first; TESTED PATHS ONLY (live-verified 2026-08-14 — cancel/modify/stop/盤後零股 raise `NotImplementedError`, extend only after a user-approved live run). **602 execution constraint: SKCOM only works under a password-logon Administrator identity — run via NSSM `ObjectName .\Administrator` or a schtasks vehicle, NEVER from the gateway shell or SSH (fails 602 even with everything installed; see `references/capital-broker.md` Step 2 + `references/manager.md` § Capital broker exception).**
- `place_futures_market_order(env, symbol, action, lots, intent)` — IOC market order (`bstrPrice='M'`, `sNewClose=2` auto); near-month aliases only (`TX00`/`MTX00`/`TM0000`); `lots` = **口數**. `intent` (`'entry'`/`'reduce'`) is REQUIRED — auto new/close can't express it, so the caller declares: entry is HALT-blocked, reduce (flattening) always passes.
- `place_stock_order(env, symbol, action, lots, price=None)` — whole-lot 整股 (sPeriod=0, ROD); `lots` = **張 (1000 shares — fill reports come back in SHARES)**; `price=None` → market, numeric → limit. The broker does NOT pre-check funding on buys — remind the user to fund the settlement account by T+2.
- `place_odd_lot_order(env, symbol, action, shares, price)` — 盤中零股 (sPeriod=4, full struct), `shares` = raw shares 1–999, LIMIT-only; reports arrive under market type `TC`.
- All prices/amounts in **TWD**. Every accepted order returns a 13-digit seq; fill confirmation comes async via `OnNewData` `D` rows deduped on (KeyNo, type, exchange seq) — connect REPLAYS the whole session day and TS rows arrive duplicated. `status='filled'` carries the **RESOLVED contract** (`TM2608`, never the `TM0000` alias you sent — always reconcile positions/fills on the resolved code) plus exchange-reported `fill_qty`/`avg_fill_price`/`fill_ids`; timeout → `status='sent'` (order ACCEPTED — a resting ROD limit is normal, and it may have filled unseen: query reports, never blindly resubmit).
- Entry orders pass through `lib/guard` (halt + audit: `order_denied_halt`/`order_sent`/`order_error`/`order_filled`); closes/sells are never trapped. **No broker attribution — 群益 is the broker itself** (no header/prefix/aff code, unlike every exchange lib).

`lib/capital_worker.py` — **Capital (群益) account snapshot worker — a SERVICE, not an import.** Runs as NSSM service `blave-agent-capital` under `.\Administrator` (password logon — same 602 constraint as above). Polls futures equity (`GetFutureRights` — rate-limited, error 1019; 60s cadence measured safe), open interest, and securities inventory over COM every 60s and writes `state/capital_account.json` (atomic replace; error snapshots on failure, then exits for a fresh NSSM COM session). Install commands: `references/capital-broker.md` Step 8. **Also has a one-shot mode `python lib/capital_worker.py --once`**: logs in, reads accounts + one tick, writes `state/capital_probe.json`, exits 0/2 — used by `capital_probe.ps1` for onboarding verification (no daemon loop, does not touch `capital_account.json`).

`lib/capital_setup.ps1` — **Capital component installer (onboarding Step 3), idempotent, one call.** VC++ 2010 + Python deps + download/extract/register the SKCOM component (proven via `CreateObject`) + stage the verify tool WITH its DLLs. Runs from the SYSTEM shell (touches no certificate); prints JSON, exit 0/1. Re-runs skip what's already registered (keyed on `CreateObject`, not a marker) and won't fight the live worker's DLL lock. `-Version` / `-Force` params. Replaces ~20 hand tool-calls (7–12 min → 2–3 min).

`lib/capital_probe.ps1` — **Capital read-only verify (onboarding Steps 5–6), one call.** Wraps `capital_worker.py --once` in the schtasks Administrator password vehicle (the only context SKCOM login passes — 602). Prints `state/capital_probe.json`; exit 0 ok / 2 failed (`stage`: login/cert/accounts) / 3 hung. **Use this instead of hand-writing a login script or checking the cert store from your own (SYSTEM) shell** — both misfired in 2026-08-20/21 onboardings.

`lib/account_capital.py` — **Capital (群益) account reader — reads the worker's snapshot file, NEVER the venue.** The platform account_reader runs as LocalSystem and cannot pass SKCOM's 602 identity check, so this lib only parses `state/capital_account.json` (missing / stale >300s / worker-error → readable `RuntimeError`, not silent zeros). `get_equity` → 權益數 in **TWD** (securities-only accounts raise — valuation not implemented; holdings still listed); `get_positions` → `{symbol: {'side','size'}}` with **size in LOTS (口), not notional** (TW futures reconcilers are hand-wired; buy/sell rows netted per symbol), symbols always the RESOLVED contract (`TM2608`), never the `TM0000` alias; `get_holdings` → securities inventory in shares, display-only, unpriced.

**When writing new reusable logic** (new exchange order helper, new alpha data fetcher, etc.):
- Add it to the appropriate `lib/` file first (or create a new one, e.g. `lib/orders_binance.py`) — never one of the five read-only backtest-chain files (AGENTS.md › Shared Library)
- Then import it in the strategy

**Strategy-specific logic** (signal computation, indicator setup, place_order for a specific exchange) stays in the strategy file.

Skill examples show data-fetch patterns only — integrate them into TEMPLATE_A.py using lib/ imports.

**Marketplace lib rule** — strategies shared via marketplace must follow this boundary strictly:
- `compute_signal`, indicator calculation, and all logic that affects trade decisions MUST stay in the strategy file — never in a custom lib
- Custom `lib/` files must only contain IO utilities (exchange order helpers, data fetchers) — logic that can be fully described by interface + behavior, not by implementation detail
- This allows recipients to reconstruct missing custom lib from the description without risking strategy inconsistency
